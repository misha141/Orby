# Copy to deploy-ec2.env (gitignored).

# Ubuntu AMI → ubuntu ; Amazon Linux → ec2-user
EC2_USER=ubuntu
EC2_HOST=YOUR_PUBLIC_IP_OR_DNS

# Run once: chmod 400 orby.pem
EC2_KEY=orby.pem

REMOTE_DIR=/home/ubuntu/orby

# Verbose SSH if login fails: DEPLOY_SSH_DEBUG=1 ./scripts/deploy-ec2.sh
